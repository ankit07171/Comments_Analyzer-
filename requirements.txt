# ------------------------------------------------------------------
# Comment Intelligence Platform — dependencies
# Lightweight multilingual version
# ------------------------------------------------------------------

# Core application
streamlit>=1.36
python-dotenv>=1.0

# Data handling
pandas>=2.2
numpy>=1.26
scikit-learn>=1.4          # For spam similarity scoring (TfidfVectorizer)

# NLP - Lightweight multilingual models
transformers>=4.35.0
torch>=2.0.0
torchvision>=0.15.0        # Required by transformers
sentencepiece>=0.1.99      # For tokenization in XLM-RoBERTa
tiktoken>=0.5.0            # For tokenization fallback

# For multilingual sentiment analysis
# cardiffnlp/twitter-xlm-roberta-base-sentiment
# This is a pre-trained model that will be downloaded automatically
# No need for fine-tuning or custom model files

# Optional: Language detection (fastText)
# Uncomment if you want better language detection
fasttext>=0.9.2

# VADER as fallback for English
vaderSentiment>=3.3.2

# Ingestion APIs
requests>=2.31
apify-client>=1.7          # Instagram comment scraping
atproto>=0.0.55            # Bluesky (AT Protocol) comment/reply ingestion

# Optional AI explanations (Gemini API)
# Only needed if GEMINI_API_KEY is set