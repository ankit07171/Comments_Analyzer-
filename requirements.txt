# ------------------------------------------------------------------
# Comment Intelligence Platform — dependencies
# ------------------------------------------------------------------

# App / UI
streamlit>=1.36
python-dotenv>=1.0

# Data
pandas>=2.2
numpy>=1.26

# NLP / classification (lightweight, no external model downloads)
vaderSentiment>=3.3.2
textblob>=0.18
scikit-learn>=1.4

# Ingestion
requests>=2.31
apify-client>=1.7          # Instagram comment scraping
atproto>=0.0.55            # Bluesky (AT Protocol) comment/reply ingestion

# Integrations (only the ones this project actually uses)
# - Jira Cloud REST API  -> requests (already listed above)
# - Upstash Redis REST   -> requests (already listed above)

# Optional AI summary / deep explanations (Gemini) — used only if
# GEMINI_API_KEY is set; app degrades gracefully without it.
